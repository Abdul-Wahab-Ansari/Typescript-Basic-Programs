// Store the names of your friends in an array
const names = ["Atta", "Asif", "Hamza"];

// Define a common greeting message
const greetingMessage = "Hello, ";

// Loop through the array and print personalized messages
for (let i = 0; i < names.length; i++) {
    //console.log(greetingMessage + names[i] + "!");
    console.log(`${greetingMessage} ${names[i]} !`);
}
